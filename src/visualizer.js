import React from "react";
// import * from D3;

const Visualizer = (props) => {
  console.log(props.data);
  // let test = new Uint8Array(200);
  // console.log(test);
  return <h1>Visualizer</h1>;
};

export default Visualizer;
